{
  "name": "@types/event-delegation",
  "description": "dom event-delegation",
  "version": "0.0.9",
  "author": "typoerr",
  "license": "MIT",
  "source": "src/index.ts",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "rx",
    "dist",
    "README.md",
    "LICENCE"
  ],
  "homepage": "https://github.com/typoerr/event-delegation#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/typoerr/event-delegation.git"
  },
  "bugs": {
    "url": "https://github.com/typoerr/event-delegation/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.7",
    "@typoerr/eslint-config": "^0.0.9",
    "eslint": "^5.16.0",
    "jest": "^24.7.1",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "tsconfig-paths": "^3.8.0",
    "typescript": "^3.4.5"
  },
  "scripts": {
    "example": "parcel example/index.html --port 3000 --out-dir example/dist --cache-dir example/.cache --open",
    "bundle": "run-p bundle:*",
    "bundle:core": "microbundle --raw -f cjs,es --no-compress",
    "bundle:min": "microbundle --raw -f cjs -o dist/index.min.js",
    "bundle:rx": "cd rx && yarn run bundle",
    "clean": "run-p clean:**",
    "clean:dist": "rimraf dist",
    "clean:rx": "rimraf rx/dist",
    "lint": "eslint src/**/*.ts",
    "release:github": "git add . && git commit -am $npm_package_version --allow-empty && git tag $npm_package_version && git push && git push --tags",
    "release": "run-s test clean bundle release:github",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "verbose": true,
    "globals": {
      "ts-jest": {
        "tsConfig": "test/tsconfig.json"
      }
    }
  }
}
